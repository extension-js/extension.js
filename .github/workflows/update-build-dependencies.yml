name: Sync build deps tracking manifest

on:
  push:
    branches: [ main ]
    paths:
      - "programs/develop/webpack/webpack-lib/build-dependencies.json"
      - ".github/build-deps/package.json"
      - "scripts/sync-build-deps-tracking.mjs"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Sync tracking manifest from build-dependencies.json
        run: node scripts/sync-build-deps-tracking.mjs

      - name: Commit changes (if any)
        run: |
          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .github/build-deps/package.json
          git commit -m "chore: sync build deps tracking manifest"
          git push
