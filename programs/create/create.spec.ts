export default function moduleSpec() {}
/**
 * can create a new extension
 * can create a new extension from a template
 * can create a new extension in a custom output directory
 * can create a new extension from a template in a custom output directory
 */
