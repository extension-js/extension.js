// Declaration twin for styles.css so its side-effect imports typecheck
// under allowArbitraryExtensions (the wildcard *.css ambient module does
// not apply to relative or path-mapped imports).
export {}
